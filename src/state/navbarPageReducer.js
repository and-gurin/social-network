let initialState = {
    friendsData: [
        {id: '1', name: 'Andrei'},
        {id: '2', name: 'Iryna'},
        {id: '3', name: 'Vanya'}
    ]
}
const navbarPageReducer = (state = initialState, action) => {
    return state
}

export default navbarPageReducer